import React, { useState } from 'react';
import { StatusBar, Text, View } from 'react-native';
import styles from './styles';

const PraisesIndexScreen = () => {
  return (
    <View style={styles.container}>
      <Text> {'تسابيح'} </Text>
    </View>
  );
};

export default PraisesIndexScreen;
