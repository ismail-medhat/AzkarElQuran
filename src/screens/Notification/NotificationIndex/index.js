import React, { useState } from 'react';
import { StatusBar, Text, View } from 'react-native';
import styles from './styles';

const NotificationIndexScreen = () => {
  return (
    <View style={styles.container}>
      <Text> {'الاشعارات'} </Text>
    </View>
  );
};

export default NotificationIndexScreen;
